public class Post
{
    private string title;
    private DateTime date;
    private string content;

    public Post(StreamReader postFile, StreamReader template)
    {

    }

    // This function takes in an output path, and uses the template to create a page
    public void WritePageTo(string path)
    {

    }
}